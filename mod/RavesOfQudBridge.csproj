<Project Sdk="Microsoft.NET.Sdk">

  <!--
    DEV-TIME TYPE-CHECK HARNESS ONLY.

    Caves of Qud compiles these .cs files itself at runtime (bundled Roslyn), so
    the mod is SHIPPED AS SOURCE — you never deploy the DLL this project builds.
    Its sole purpose is to compile the mod against the game's real assemblies so
    API mismatches surface here instead of at game launch.

    Build:  dotnet build mod/RavesOfQudBridge.csproj
    Point at a different install:
            dotnet build mod/RavesOfQudBridge.csproj -p:CoQManaged="/path/to/Managed"
  -->

  <PropertyGroup>
    <TargetFramework>netstandard2.1</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>disable</Nullable>
    <EnableDefaultItems>false</EnableDefaultItems>
    <!-- We never ship this output; don't drag Unity DLLs into bin/. -->
    <CopyLocalLockFileAssemblies>false</CopyLocalLockFileAssemblies>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>

    <!--
      WHERE QUD'S ASSEMBLIES ARE. This defaulted to one absolute path under a home directory
      that is not yours, so the README's own type-check command failed on every other machine.
      Steam's default install per OS now, and -p:CoQManaged=... still wins over both.
    -->
    <CoQManaged Condition="'$(CoQManaged)' == '' AND '$([MSBuild]::IsOSPlatform(OSX))' == 'true'">$(HOME)/Library/Application Support/Steam/steamapps/common/Caves of Qud/CoQ.app/Contents/Resources/Data/Managed</CoQManaged>
    <CoQManaged Condition="'$(CoQManaged)' == '' AND '$([MSBuild]::IsOSPlatform(Windows))' == 'true'">C:\Program Files (x86)\Steam\steamapps\common\Caves of Qud\CoQ_Data\Managed</CoQManaged>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="*.cs" />
  </ItemGroup>

  <!--
    Reference the game's own assemblies. Private=false so they're used for
    compilation only and not copied. If the compiler reports an unresolved type
    from some UnityEngine.*Module or XRL dependency, add that DLL from $(CoQManaged)
    here — do NOT reference the game's mscorlib/netstandard/System (the SDK supplies
    those; duplicating them causes conflicts).
  -->
  <ItemGroup>
    <Reference Include="$(CoQManaged)/Assembly-CSharp.dll" Private="false" />
    <Reference Include="$(CoQManaged)/Unity.InputSystem.dll" Private="false" />
    <Reference Include="$(CoQManaged)/UnityEngine.CoreModule.dll" Private="false" />
    <Reference Include="$(CoQManaged)/UnityEngine.dll" Private="false" />
    <Reference Include="$(CoQManaged)/UnityEngine.ImageConversionModule.dll" Private="false" />
    <Reference Include="$(CoQManaged)/UnityEngine.UI.dll" Private="false" />
    <Reference Include="$(CoQManaged)/UnityEngine.UIModule.dll" Private="false" />
    <Reference Include="$(CoQManaged)/UnityEngine.ScreenCaptureModule.dll" Private="false" />
    <Reference Include="$(CoQManaged)/Unity.TextMeshPro.dll" Private="false" />  <!-- ControlledTMPInputField (popup text-entry) -->
    <Reference Include="$(CoQManaged)/UnityEngine.InputLegacyModule.dll" Private="false" />  <!-- Input.mousePosition transitive -->
    <Reference Include="$(CoQManaged)/UnityEngine.TextRenderingModule.dll" Private="false" />  <!-- TMP transitive -->
    <Reference Include="$(CoQManaged)/UnityEngine.IMGUIModule.dll" Private="false" />  <!-- TMP transitive -->
    <Reference Include="$(CoQManaged)/UnityEngine.TextCoreTextEngineModule.dll" Private="false" />  <!-- TMP transitive -->
    <Reference Include="$(CoQManaged)/UnityEngine.TextCoreFontEngineModule.dll" Private="false" />  <!-- TMP transitive -->
  </ItemGroup>

</Project>
